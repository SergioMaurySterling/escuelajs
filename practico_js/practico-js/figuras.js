var squareSide =  parseInt(prompt("Cuanto mide un lado del cuadrado?"))

function calcSquare (squareSide) {
    squareSide = squareSide
    var area = squareSide * squareSide
    var perimeter = squareSide * 4
    console.log(`El area del cuadrado es: ${area}`)
    console.log(`El perimetro del cuadrado es: ${perimeter}`)
}
calcSquare(squareSide)

var triangleSide1 = parseInt(prompt("Lado 1 del triangulo"))
var triangleSide2 = parseInt(prompt("Lado 2 del triangulo"))
var triangleBase = parseInt(prompt("Base del triangulo"))


calcTriangle = (triangleSide1, triangleSide2, triangleBase) => {
    triangleSide1 = triangleSide1
    triangleSide2 = triangleSide2
    triangleBase = triangleBase

    var triangleHeight 
    var area = (triangleBase * triangleHeight)/2
    var perimeter = triangleSide1 + triangleSide2 + triangleSide3
}

